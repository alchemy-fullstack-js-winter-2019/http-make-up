const { getColor } = require('../services/colorApi');

const post = (req, res) => {
  const {
    name,
    hex,
    rgb,
    __v,
    _id
  } = req.body;
 
const methods = {
  post,
};

module.exports ={
  
};
